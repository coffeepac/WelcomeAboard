test:
  override:
    - echo "eat a dick, mrs. stanwick"

deployment:
  demo:
    branch: master
    commands:
      - curl -XPOST https://circleci.com/api/v1/project/coffeepac/grabbag/tree/master?circle-token=$CIRCLE_TOKEN
